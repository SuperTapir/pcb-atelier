## 1. 工程基础与参考验证

- [x] 1.1 初始化 Tauri 2、React、TypeScript、Tailwind CSS v4 与 shadcn/ui 的应用骨架，并建立独立 Rust Core crate 与前端之间的最小命令边界
- [x] 1.2 实现 React + Konva 可执行性能基准，覆盖图片、文字、选框、多选、变换、六张生产层纹理和两百对象连续平移/缩放；验证交互不持续低于 45 FPS 且重复切面/显隐后内存不持续增长
- [x] 1.3 为 UI 领域命令建立 revision/失效结果防护，避免连续画布操作以旧结果覆盖新状态
- [x] 1.4 为工程、预览和导出写入临时文件校验后原子替换的基础设施

## 2. 卡片工程与命令模型

- [x] 2.1 在 Rust Core 定义带版本的 AtelierDocument、CardFace、ContentLayer、LayerGroup、TransformUm、StackupPreset、ProductionMapping 和基础板框类型
- [x] 2.2 定义六个生产层、板框、钻孔与阻焊开窗的明确枚举和不变量，禁止以屏幕镜像或反相表达领域语义
- [x] 2.3 实现图片与文字资产引用、嵌入项目包、加载、保存及 schema 验证
- [x] 2.4 实现 InsertLayer、DeleteLayer、TransformLayer、SetLayerLock、SetLayerVisibility、ReorderLayer、GroupLayer、UngroupLayer 和 MapLayer 领域命令
- [x] 2.5 实现命令历史、撤销、重做和 CLI 等价入口，并为所有基础命令编写 Rust 单元测试
- [x] 2.6 实现 SetBoardOutline/SetStackup 板体命令、保持对象物理尺寸的越界诊断，以及“每面最多一个基础铺铜”的领域不变量和重复创建返回既有 ID
- [x] 2.7 提取 Tauri 与 Web 共用的 Rust WorkspaceService 与版本化传输契约，禁止两个运行面各自重写命令语义

## 3. 生产层对象编辑工作区

- [x] 3.1 构建正背面同时显示的毫米双画板、响应式并排/上下布局、活动卡面、两面独立选择与视口、同时查看/聚焦当前面，以及互相正交的工具与编辑/预览模式
- [x] 3.2 实现文件/拖放/粘贴图片导入、默认适配居中、保留变换与映射的资产替换，以及基于 DOM `textarea` 覆盖层的点文字/定宽文字插入编辑；插入目标必须跟随活动卡面且不改变另一面选择
- [x] 3.3 实现画布与内容树联动的选择、多选、父级优先与下钻/重叠候选选择、拖动、缩放、旋转、镜像、排序、分组、显隐与锁定；锁定对象画布穿透但内容树仍可访问
- [x] 3.4 实现检查器中的精确毫米位置、尺寸、旋转、关联与导出参与状态编辑，键盘微调，以及网格、板边、对象边/中心的可见吸附和临时绕过；锁定状态阻止所有变换
- [x] 3.5 为前端编辑交互、选择路径、锁定穿透、显隐/导出分离、双画板同时显示、活动卡面与插入目标、独立选择/视口、布局保持、编辑/预览模式正交及撤销/重做编写 Vitest 测试
- [x] 3.6 将左侧重构为“板体 → 正面/背面 → 铜/阻焊开窗/丝印”，删除独立内容入口；对象直接显示在生产层下并明确多层关联身份
- [x] 3.7 实现按生产层限制的插入入口、自动合法映射、关联到其他生产层高级操作，以及重复基础铺铜选中既有对象
- [x] 3.8 实现板体检查器的宽高、圆角、板厚、阻焊颜色、表面处理和越界警告；FR-4 固定且不显示主要材料选项

## 4. 生产层编译与 2D 检查

- [x] 4.1 实现 AtelierDocument → FabricationPlan → ResolvedFabricationBoard 统一编译器，以固定生产网格将图片/文字确定性解析为顶/底铜、阻焊开窗、丝印 bit mask、板框和基础孔，并记录输入哈希与栅格器/字体版本
- [x] 4.2 在编辑模式统一左侧树实现派生且只读的生产层映射检查：选择目标层、添加/移除映射、追溯来源、临时显隐/隔离，以及基础叠层（基材、阻焊颜色、表面处理）选择
- [x] 4.3 定义可替换的 ProductionRenderer 与 BoardPreviewRenderer 接口；用 Konva 纹理层实现仅消费 ResolvedFabricationBoard 的编辑态 2D 单层检查，并用 Three.js 实现消费同一生产结果的整体 3D 成板预览、轨道旋转/缩放/平移和正反面观看方向
- [x] 4.4 创建带 `F` / `B` 标记、偏心特征、独立图片和文字的非对称黄金卡片 fixture
- [x] 4.5 编写编译器测试，验证物理尺寸、原点、板框裁切、阻焊开窗极性、生产层映射及正反面方向

## 5. 嘉立创 EDA 交接

- [x] 5.1 从 PCB_lightgraph Neo 提炼可复用的公开 EDA 归档生成、`.epro2 → .eprj2` 转换和结构验证代码，移植到新 Core 的 FabricationPlan 接口而不移植旧 LayerSet 模型
- [x] 5.2 将首版支持的六层语义映射为嘉立创 EDA 静态生产图元；对暂不支持的层、孔槽或映射实现明确的导出前错误
- [x] 5.3 实现可追溯的导出版本、原子写入和导出结果报告
- [x] 5.4 为黄金卡片编写 EDA 导出测试，验证 `.eprj2` 结构、尺寸、层语义、阻焊开窗极性及正反面非对称方向
- [x] 5.5 在嘉立创 EDA 中人工打开黄金导出工程，记录层、方向、尺寸和可编辑图元的验收结果

## 6. 端到端验收与文档

- [x] 6.1 编写桌面 E2E：同时显示正背画板、切换活动卡面、插入图片和文字、独立选择/视口、聚焦恢复、变换/锁定、映射顶/底生产层、检查编辑态 2D 层与只读 3D 预览并导出嘉立创 EDA 工程
- [x] 6.2 验证 GUI 与 CLI 对同一工程生成等价领域文档和生产层几何
- [x] 6.3 补充用户操作说明、工程 schema 说明、导出限制与“EDA 为单向下游出口”的行为说明
- [x] 6.4 运行 Rust、Vitest、Playwright、双画板 Konva 性能基准、Three.js 预览交互与黄金导出验证，并记录首个端到端验收报告
- [x] 6.5 移除 Web 的硬编码 `8 × 5` 预览和不完整测试 IPC；验证 Web 图片插入、分组/解组、真实 ResolvedFabricationBoard 预览及与 Tauri 的契约/哈希等价
- [x] 6.6 修复桌面预览性能与一致性：预览纹理使用 PNG 紧凑传输，按工程 revision 缓存并复用 ResolvedFabricationBoard，Tauri 在后台线程编译，编辑态合并刷新；以 Rust 像素测试和浏览器截图验证图片、文字、铺铜及阻焊颜色进入真实 3D 成板纹理
