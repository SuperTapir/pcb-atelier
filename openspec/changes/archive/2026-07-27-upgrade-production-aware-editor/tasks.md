## 1. 建立回归基线与领域模型

- [x] 1.1 为当前 schema 图片工程、正背面非对称图形、铜/阻焊/丝印组合建立生产 mask、物理边界和 EasyEDA 导出黄金 fixture
- [x] 1.2 在 `atelier-core` 定义 `ProjectAsset`、`ImageTreatment`、`TreatmentRecipe`、图片实例引用和扩展后的 `ProductionMapping`
- [x] 1.3 定义版本化 `ManufacturerProfileSnapshot` 及 FR-4、板厚、外层铜厚、阻焊、字符工艺和表面处理稳定枚举
- [x] 1.4 定义唯一的当前工程 schema 与严格完整性校验，拒绝旧图片、旧阈值、旧制造字段和旧能力快照，确保派生缩略图和代理缓存不成为领域真相
- [x] 1.5 实现带字段路径错误的严格加载与原子保存，验证解析或校验失败时磁盘文件和当前内存工作区均保持不变
- [x] 1.6 为素材、处理版本、实例、映射和制造配置领域命令补齐撤销/重做、归档往返与 Rust 单元测试

## 2. 实现非破坏式图片处理核心

- [x] 2.1 从旧项目参考实现中筛选并移植解码方向归一、灰度、Otsu 初始估算、可编辑阈值和反相算法，不引入旧 UI 与单图工作流
- [x] 2.2 实现 alpha/白底语义、裁切、平滑及其版本化配方解释器，并为每个选项加入可复现 fixture
- [x] 2.3 实现以毫米或平方毫米表达的去斑、最小区域、最小线宽和最小间隙诊断/修复
- [x] 2.4 实现交互代理、成板预览和正式生产三级采样 API，返回配方指纹、revision、物理边界和诊断信息
- [x] 2.5 实现由资产哈希、配方、裁切、物理尺寸、采样间距和栅格器版本组成的缓存键，并证明平移/旋转不会使局部 mask 失效
- [x] 2.6 用非对称图片、孤岛、细线、孔洞和反相样例验证三级输出的拓扑、极性、方向及物理容差一致
- [x] 2.7 为图片处理增加必填 `productionMode`，将 `colorOriginal` 严格限制为真实 PNG/JPEG、丝印目标和合法彩色丝印制造配置，并让修改进入撤销/重做历史

## 3. 项目媒体库与共享工作区契约

- [x] 3.1 实现项目内素材导入、SHA-256 去重、嵌入归档、文件夹组织、搜索元数据、使用次数和未使用状态
- [x] 3.2 实现“替换当前实例”“替换全部引用”“删除实例”“删除资产”和“清理未使用素材”的独立领域操作与保护提示
- [x] 3.3 扩展 Tauri、Web 测试适配器和 CLI 契约，使三者调用相同的素材、处理版本、实例放置和制造配置领域操作
- [x] 3.4 实现后台处理调度、对象级请求合并、并发上限、取消和 revision/配方指纹防陈旧写回
- [x] 3.5 为 CLI 增加可脚本化的素材导入、处理配方编译、制造配置检查和生产输出追溯能力
- [x] 3.6 验证工程在移除原始磁盘文件与清空代理缓存后仍可重建编辑代理、3D 纹理和正式生产输出

## 4. 大屏工作区与媒体库界面

- [x] 4.1 将左侧重构为生产层树与项目媒体库同时可见的纵向可调整 Dock，并持久化两区尺寸、折叠、滚动和展开状态
- [x] 4.2 将画板视图合并为“左右/上下/聚焦当前面”三状态选择器，首次默认左右，让顶部选择器与设置页读写同一 `canvasView` 本地偏好，并在每次切换布局后按新可用区域自动适配画板
- [x] 4.3 实现承载默认画板视图、主题、启动窗口方式、滚轮缩放阻尼及后续全局偏好的设置页；启动默认窗口化全屏，阻尼提供高/中/低并默认中，验证偏好跨重启和跨工程恢复且不写入 `.pcba`、不触发工程脏状态
- [x] 4.4 让三状态选择器按整个应用窗口几何居中，编辑/预览固定在顶部右侧且切换不跳动，并从顶栏移除撤销/重做但保留菜单和快捷键
- [x] 4.5 在 1920 和 1440 CSS px 布局下保证生产层、媒体库、双画板和检查器可同时工作，并实现窄窗口的非破坏式折叠降级
- [x] 4.6 实现媒体库缩略图/列表、文件夹、搜索、尺寸/格式/透明通道、使用次数和处理版本展示
- [x] 4.7 实现三条拖放路径：外部图片到画板直接导入并放置到落点卡面/当前生产层，外部图片到媒体库仅导入资产，媒体库素材到画板复用资产并放置实例；失败不得留下半完成映射
- [x] 4.8 实现图片导入处理器与后续检查器中的原图/结果并排对照、参数防抖、诊断标记和确认插入流程
- [x] 4.9 实现显式“临时查看原图”，保证它不改变选择框、物理变换、处理配方或导出结果

## 5. 生产近似画布与流畅交互

- [x] 5.1 将图片编辑节点改为对象级生产代理 mask，并按当前铜、阻焊、开窗和丝印材质近似着色
- [x] 5.2 移除同一对象的原图与整板生产纹理重复绘制路径，确保多生产层关联只有一个命中目标、选择框和变换
- [x] 5.3 让拖动、缩放和旋转只更新客户端节点矩阵，手势结束时提交单个领域命令并异步更新复合结果
- [x] 5.4 实现独立指针状态机：左键选择/框选/移动，右键候选/平移，右键原地释放打开对象或画布菜单
- [x] 5.5 实现消费连续滚轮/触控板 delta 的指针锚定缩放、高/中/低阻尼响应系数、正背面独立视口，以及点击外部、Esc、视口变化和命令执行后关闭上下文菜单
- [x] 5.6 在发布构建中加入交互测量，验证拖动和平移不触发生产编译或同步 IPC，帧更新 p95 达到 16.7 ms 目标
- [x] 5.7 精简画布 HUD，只保留正面/背面和缩放百分比，移除板内 `FRONT`/`BACK`、常驻手势提示、坐标读数、画板尺寸和生产层 badge，并由检查器承担精确属性

## 6. 统一命令、快捷键与对象菜单

- [x] 6.1 建立包含命令 ID、中文标题、快捷键、作用域、启用条件和执行函数的统一命令注册表
- [x] 6.2 接入 `V`、`T`、`P`、Delete/Backspace、复制粘贴、创建副本、方向键微调、组合/解组和撤销/重做
- [x] 6.3 在输入框、重命名、文字编辑、数值控件和模态窗口聚焦时抑制画布单键与删除快捷键
- [x] 6.4 让对象右键菜单、图层菜单、插入入口和检查器复用注册表命令及相同的锁定、撤销和状态反馈
- [x] 6.5 明确区分“移出当前生产层”和“删除对象”，并在删除多层关联对象前展示影响范围
- [x] 6.6 实现创建副本时复用素材与处理版本、创建新实例 ID、复制合法映射并施加可见物理偏移
- [x] 6.7 移除图层面板底部常驻大号分组/解组按钮，改为仅在多选可组合或选中组合时出现的紧凑上下文操作条，无可用动作时不占空间，并复用统一命令注册表

## 7. 制造配置与工艺近似

- [x] 7.1 建立带官方资料来源和版本日期的嘉立创 FR-4 能力快照，覆盖首版阻焊、字符、板厚、外层铜厚和表面处理选项，并明确拒绝仅铜基板支持的 OSP 与当前 FR-4 不支持的 `0.5 oz`
- [x] 7.2 实现制造组合规则校验，区分标准黑白丝印和彩色丝印，并在入口处解释和阻止不合法组合
- [x] 7.3 在板体检查器提供制造参数编辑，同时保持 FR-4 为首版固定基材且不暴露含糊的“板体颜色”
- [x] 7.4 让阻焊、字符、基材和露铜表面处理的工艺近似色在正背编辑画板即时更新且不重建生产几何
- [x] 7.5 验证白色阻焊、嘉立创紫、沉金、喷锡及“开窗有铜/无铜”在编辑与 3D 中的材质语义

## 8. 预览、EasyEDA 与跨运行时一致性

- [x] 8.1 让 2D 生产检查和 3D 成板预览只消费当前 revision 的真实 `ResolvedFabricationBoard`，删除硬编码或状态无关纹理
- [x] 8.2 让 3D 预览复用制造调色板和图片处理配方，并在冷编译期间保持可交互、显示进度且拒绝陈旧结果
- [x] 8.3 更新 EasyEDA 适配器，使其只消费正式精度生产 mask、稳定物理坐标和已验证制造配置
- [x] 8.4 在 EasyEDA 导出报告中记录源实例、目标层、处理版本、算法版本、资产哈希、制造摘要和支持状态
- [x] 8.5 对适配器无法无损表达的彩色丝印或制造组合阻止“一键下单”声明，并提供可操作的降级说明
- [x] 8.6 对 Rust、CLI、Web 与 Tauri 输出运行黄金比较，验证 mask、边界、拓扑、极性、正背方向和指纹一致
- [x] 8.7 对合法 `colorOriginal` 丝印逐映射输出原始 PNG/JPEG sidecar 和含哈希、目标面、裁切、物理变换的 manifest，显式要求 EasyEDA Pro 继续导出 `FCTS`/`FCBS` 且不得伪造直接下单成功

## 9. 自动化与实际产品验收

- [x] 9.1 增加 Web 组件与 E2E 测试，覆盖三状态选择器默认左右及跨重启恢复、聚焦切面状态保留、主题、启动窗口方式和默认中档缩放阻尼恢复、设置不污染工程、整窗几何居中、编辑/预览右侧切换不跳、HUD 只显示正/背与缩放百分比、素材复用、原图/结果处理和单一选择框
- [x] 9.2 增加真实指针与拖放 E2E，覆盖左键框选/移动、右键平移、右键菜单阈值、菜单失焦关闭、正背独立视口、连续 delta 缩放与指针锚定、高/中/低阻尼速度关系，以及外部图片到画板、外部图片到媒体库、媒体库素材到画板三条拖放路径和无效目标原子拒绝
- [x] 9.3 增加快捷键与选择上下文 E2E，覆盖 Delete、V/T/P、复制/副本、组合/解组、顶栏无撤销/重做时菜单与快捷键仍可撤销重做、文本输入作用域隔离，以及图层面板紧凑操作条仅在动作可用时出现且与右键/快捷键结果一致
- [x] 9.4 增加制造参数 E2E，覆盖阻焊即时换色、表面处理只换材质、FR-4 拒绝 OSP/`0.5 oz` 和其他无效组合前置拦截
- [x] 9.5 在浏览器中完成完整创作流程验证：从外部拖图直接放置、从媒体库复用素材、处理、双面排版、多生产层关联、预览和 EasyEDA 导出
- [x] 9.6 在 Tauri 发布构建中完成实际交互验证，记录拖动、插入、铺铜、编辑/预览切换、3D 操作和工程重开的性能数据
- [x] 9.7 对照黄金 fixture 检查浏览器与 Tauri 的视觉和生产结果，修复所有断链后再将本变更标记完成

## 10. 图片矢量轮廓质量

- [x] 10.1 为斜线、曲线、孔洞、小岛和退化边界增加 EasyEDA 矢量化失败用例，断言最大物理偏差、绕向、非自交和拓扑不变
- [x] 10.2 实现闭合轮廓的确定性有界简化，并在候选结果不安全时按环回退到正式生产像素边界
- [x] 10.3 验证 EasyEDA 静态 `FILL` 输出不再逐像素阶梯，运行 Rust 黄金测试、OpenSpec 校验和相关跨运行时回归
- [x] 10.4 让编辑画布按缩放档位请求 250/100/50/25 µm 对象代理，并验证缓存键与最高缩放显示密度
- [x] 10.5 根据真实 EasyEDA 对照工程补充多像素平台反例，实现亚像素非轴向轮廓，并将同层岛屿聚合为单一图元
