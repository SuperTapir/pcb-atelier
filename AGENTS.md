# Project instructions

## OpenSpec language

- OpenSpec 文案优先使用中文，包括 `proposal.md`、`design.md`、`specs/**/spec.md`
  和 `tasks.md`。
- 技术标识符、代码符号、文件路径、命令、格式名称与无法准确翻译的专有名词保留英文。
- 需求中的规范性措辞和验收场景应使用清晰、可测试的中文表达。

## Engineering methodology

- 修改项目内容、开发功能、重构、修复问题或补充测试时，默认使用项目级
  `tapir-craft` skill 作为工程方法论。
- TDD 是默认开发方式：在实现前先把目标或问题转化为可重复验证的失败用例，并优先
  落成自动化测试；不适合单元测试时，应先定义集成测试、E2E case、可执行 repro
  或步骤明确的手工验证 case。
- 测试应验证用户可观察行为和稳定契约，避免为了追求覆盖率而绑定实现细节。
- 实现后必须运行相关测试与必要的质量检查；若验证失败，应回到假设、设计与实现继续
  迭代，不能以“代码看起来正确”作为完成标准。
- 开发前识别方案的 trade-off、side effect 与影响范围。若局部修改和全局治理都成立，
  应明确两者差异，不得未经授权将小改动扩大成大重构。

## Product research and references

- 进行产品定义、交互设计、架构选型或复杂功能开发前，可以进行大规模调研；优先研究
  同类产品、成熟开源项目、官方文档、公开技术文章与市面最佳实践，不凭印象重复造轮子。
- 对关键能力应尽量进行源码级学习，理解其数据模型、交互状态、性能策略、测试方法和
  已知 trade-off；借鉴经过验证的工程经验，但不得直接照搬与本项目目标不符的架构。
- 调研所需的开源项目、示例、文档、截图、测试素材和其他资源可以放入 `reference/`
  文件夹。可以在其中克隆完整项目或保存必要资料，但第三方源码不得混入产品源码，
  也不得未经明确决策成为运行时依赖。
- 新增重要参考资料时，应在 `reference/README.md` 记录来源 URL、许可证、所用
  commit/tag、调研目的和可借鉴结论；引用或复用代码前必须确认许可证与署名要求。
- 调研结论应回到本项目的真实用户流程和验收 case 中验证。市场惯例和开源实现是证据，
  不是自动成立的产品需求。
